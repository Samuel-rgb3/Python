import time

segundos = 0
try:
    while True:
        minutos = segundos // 60
        secs = segundos % 60
        print(f"{minutos:02d}:{secs:02d}", end="\r")
        time.sleep(1)
        segundos += 1
except KeyboardInterrupt:
    print("\nCronômetro parado.")
